<Window x:Class="StockManagement.WPFClient.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Product" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Product Name:" Margin="5"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Margin="5"/>
        
        <TextBlock Grid.Row="2" Text="SKU:" Margin="5"/>
        <TextBox Grid.Row="3" x:Name="SkuTextBox" Margin="5"/>
        
        <TextBlock Grid.Row="4" Text="Price:" Margin="5"/>
        <TextBox Grid.Row="5" x:Name="PriceTextBox" Margin="5"/>
        
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OK_Click" Margin="5" Padding="10" IsDefault="True"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="5" Padding="10" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>